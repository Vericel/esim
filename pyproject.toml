[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "esim"
version = "0.1.0"
description = "Verilog/SystemVerilog filelist flattener"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["rich>=14.3,<15"]

[project.scripts]
ff = "ff.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
