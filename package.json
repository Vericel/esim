{
  "name": "esim-quality-tooling",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "typecheck": "pyright --project pyrightconfig.json && pyright --project pyrightconfig.tests.json"
  },
  "devDependencies": {
    "pyright": "1.1.413"
  }
}
